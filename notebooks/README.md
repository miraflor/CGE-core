# CGE-Core notebooks

Run these in order if you are learning the system. The first six are user-facing; `90_internals.ipynb` is deliberately advanced.

Public notebooks contain no Git checkout/reset logic, `sys.path` manipulation, repository-root changes, PATH injection, or solver-selection code. A Colab notebook may contain one package installation cell.
