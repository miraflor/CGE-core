# Getting Started

CGE-Core is a Python/Pyomo framework for computable general equilibrium
modelling and reproducible policy simulation.

The basic workflow is:

**load data → calibrate → define a shock → solve → compare**

Start with the {doc}`../workflow` page for installation, model setup,
simulation, and result comparison.
