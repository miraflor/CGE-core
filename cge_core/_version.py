"""CGE-Core package version.

Keep this value synchronized with ``project.version`` in ``pyproject.toml``.
A source checkout must not inherit stale version metadata from an older
installed CGE-Core distribution in the active Python environment.
"""

__version__ = "0.8.0"
