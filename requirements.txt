# Runtime dependencies for CGE-Core.
# The authoritative dependency declaration is pyproject.toml; this file is
# a convenience for `pip install -r requirements.txt` workflows.
pyomo>=6.0,<6.10; python_version < "3.10"
pyomo>=6.0; python_version >= "3.10"
dill>=0.3
pandas>=1.3
amplpy>=0.16,<1

# CGE-Core resolves a supported nonlinear backend through its normal solver
# layer. Advanced users may still provide a local IPOPT/cyipopt installation
# and request it explicitly with solve(solver="...").
